# This folder contains symlinks

It's basically a way of getting smaller absolute paths in various text editors,
most importantly the acme editor

## Setup

In MacOS, link this folder to a root folder using synthetic folders:

- Edit or create `/etc/synthetic.conf` and add 

> l	/Users/YourUsername/ThisFolder
