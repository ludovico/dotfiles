- defaults:
    link:
        relink: true

- clean: 
    ~/:
    ~/.config:
        recursive: true

- link:
    ~/.config/nvim:
        create: true
        path: nvim
    ~/.dotfiles: ''
    ~/.tmux.conf: tmux.conf
    ~/.zshrc: zshrc
    ~/.zprofile: zprofile
    ~/bin: bin
    ~/.xinitrc:
        if: '[ `uname` = Linux ]'
        path: xinitrc

- create:
    - ~/.zsh/histfile

- shell:
    - [git submodule update --init --recursive, Installing submodules]
